{
  "name": "onboarding-feedback-ui",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "http-proxy-middleware": "^3.0.5",
    "node-fetch": "^2.7.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "test:unit": "react-scripts test --watchAll=false",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:ui": "playwright test --ui",
    "test:coverage": "react-scripts test --coverage --watchAll=false",
    "test:e2e:clean": "npx kill-port 3000 && playwright test",
    "mcp:server": "npx @playwright/mcp --host localhost --port 3001 --browser chrome --output-dir ./test-results/mcp-sessions --save-trace --save-video 1280x720",
    "mcp:debug": "npm-run-all --parallel start mcp:server",
    "mcp:setup": "echo 'Starting MCP debugging session...' && mkdir -p test-results/mcp-sessions",
    "mcp:troubleshoot": "node scripts/troubleshoot-mcp.js",
    "mcp:quick-start": "node scripts/quick-start-mcp.js"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@playwright/mcp": "latest",
    "@playwright/test": "^1.55.0",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^24.5.2",
    "autoprefixer": "^10.4.21",
    "jest-environment-jsdom": "^30.1.2",
    "kill-port": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "wait-on": "^9.0.1"
  }
}
